# 07 — Proposed Spec Mutations

## Proposed Spec Mutations

| Section               | Current text                                     | Proposed change                                                                                                                                             | Rationale                                                                                                   | Confidence                                                        |
| --------------------- | ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| §1 (harness contract) | "Read plans/CONTINUATION_STATE.md first"         | Add: "and `git status --short` + `git log --oneline -3` — uncommitted work is invisible to state files"                                                     | Session N+1 found 32 uncommitted files despite "DEPLOYED & VERIFIED, no outstanding bugs" state             | [DERIVED] observed twice (interrupted sessions leave dirty trees) |
| §8 (testing)          | "re-run from a clean state"                      | Add: "performance/size gates should assert wire metrics (gzip/brotli), with raw ceilings only as bloat guards"                                              | Raw-byte gates false-positive on by-design aggregation; gzip is what users pay for                          | [DERIVED]                                                         |
| §5 (research)         | "verify against live internet"                   | Add repo-local corollary: "verify cross-file dependencies (CSS↔JS creators, HTML↔manifest generators) by reference-grep before declaring a wiring complete" | scroll-progress CSS existed in 29 pages with 1 creator; grep found it in minutes                            | [DERIVED]                                                         |
| §13 (macro-audit)     | checklist item "All tests pass from clean state" | Add: "axe serious/critical = 0 on key pages" as an explicit standing gate                                                                                   | It was the de-facto bar in prior sessions but unwritten; this session restored it after a silent regression | [HIGH-CONF]                                                       |

(No axiom mutations proposed. All four are procedural additions consistent with observed failure modes; none applied to the spec file itself this session — proposer lacks write access to the agent spec from inside the workspace.)
