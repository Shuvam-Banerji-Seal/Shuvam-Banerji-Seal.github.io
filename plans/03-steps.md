# 03 — Steps (kept current)

## Rev 5 — 2026-09-18 — Fullscreen Timer app (new feature wave)

| Step   | Input                                            | Output                                                                                                                   | Method                                                                                                 | Test                                                                                                                                                             | Risk / fallback                                                                         |
| ------ | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| Spec   | User request + alphakevin/fullscreen-timer (MIT) | plans/08-fullscreen-timer.md, feature list locked                                                                        | Fetch README + src/App.js this session; map reference behaviours to our decisions                      | Spec reviewed against user's four asks (negative timer, buzzer, crimson, sequences)                                                                              | Do not copy reference clamp-at-zero behaviour — user wants overtime                     |
| Engine | Spec §2                                          | assets/js/fullscreen-timer.js (parse/format, rAF + wall-clock, Web-Audio buzzer, segments, keys, wake lock, persistence) | Vanilla ES module, no bare imports; `export`ed pure helpers; `window.__fullscreenTimer` handle for E2E | node --check; 3 Playwright matrices (behaviour, states, media)                                                                                                   | rAF throttling in background tabs → 1 s watchdog interval also checks the zero-crossing |
| UI     | Spec §3                                          | pages/tools/fullscreen-timer.html + assets/css/fullscreen-timer.css                                                      | tool-page shell, setup grid + fixed stage overlay, solid colours, 4-col row grid → restacked on mobile | Screenshots 1440/390; overflow matrix; 9-theme contrast matrix                                                                                                   | Row grid mismatch (5 children vs 4 columns) caught by reading own CSS before rendering  |
| Wiring | Site conventions                                 | navbar Apps entry, tools.html card + openTool map, vite rollup input, sitemap URL                                        | Mirror pdf-studio/audio-studio registration path                                                       | 3 new comprehensive assertions + 21 new tests                                                                                                                    | Missing vite input would silently drop the page from dist                               |
| E2E    | dist build on :8091                              | Behaviour + state + media matrices                                                                                       | Instrumented AudioContext (`createOscillator` → `setValueAtTime` log)                                  | Buzzer exactly 3×880 + 3×659.25 once per crossing, ticks only ≤10 s, mute suppresses, auto-advance, stopwatch, persistence, typing guard, arrows, reduced motion | Judged colours only after the 0.25 s transition settles                                 |
| Bug    | Zoomed control screenshot                        | Primary pill invisible (cyan on cyan)                                                                                    | `.ft-ctl[aria-pressed="true"]` (later, equal specificity) overrode `.ft-ctl.is-primary`                | Fixed with `:not(.is-primary)` + `--ft-on-accent`; 9/9 themes ≥ 5.79:1                                                                                           | Screenshot review is load-bearing — computed styles alone passed                        |

## Rev 4 — 2026-09-18 — homepage UI wave (hero rhythm + single-divider headers)

| Step                   | Input                                                     | Output                                                      | Method                                                                      | Test                                                                                                     | Risk / fallback                                               |
| ---------------------- | --------------------------------------------------------- | ----------------------------------------------------------- | --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| Hero claim rhythm      | Desktop + 390px production screenshots                    | Name/role split, left mobile, 32/24px CTA→panel gap         | Scoped `#main-content` rules in index-overrides.css; MDN text-wrap verified | Vite preview screenshots + 9-theme × 3-width overflow matrix + CTA hrefs + terminal input                | Shared hero-kicker/mobile !important; inspect computed styles |
| CTA polish             | Same                                                      | Focus-visible outline + hover lift (reduced-motion guarded) | Scoped `#main-content` pseudo rules, no gradient                            | Keyboard focus render + suites                                                                           | Pre-existing global focus system untouched                    |
| Single-divider headers | Settled About/Caps/Contact screenshots showing double bar | Neutralize legacy title::after + divider sweep              | `content:none` rule; specificity beats theme background-only rules          | 9/9 themes pseudo=none, divider 60px aligned, dist bundle contains minified rule, suites + build 206/206 | Minifier rewrites `::after`→`:after`; grep dist accordingly   |

## Rev 3 — 2026-09-17 — deployed browser follow-up

| Step                  | Input                        | Output                                           | Method                                                                        | Test                                                                                                 | Risk / fallback                                                            |
| --------------------- | ---------------------------- | ------------------------------------------------ | ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| API search            | Deployed hub                 | Evidence, no code change                         | Inspect committed handler; assert results container instead of category cards | Three reloads: weather=20, hidden categories, clear=48; all passed                                   | Do not interpret hidden categories as failed search                        |
| Metadata blank fields | One-page PDF with title only | Valid saved metadata with empty optional strings | Remove undefined fallbacks; use [] for empty keywords                         | Same Playwright fixture on local page; read download bytes with updateMetadata:false; repeat 3 times | Existing nonempty values must survive; keep fix uncommitted pending review |
| State                 | Browser and suite evidence   | Accurate partial audit                           | Preserve history, distinguish local from deployed                             | Read updated records                                                                                 | No new deployment inferred from a generic continue request                 |

## Rev 1 — Loop structure (completed)

1. Read all repo files (structure, page inventory, scripts, configs)
2. Create plans/ + .rigor-trash discipline
3. Spin up dev server + Playwright MCP harness (dev) and preview (prod build sim)
4. Wave 1 crawl: all 29 pages — errors/404s collected
5. Wave 2 feature tests: every tool page interacted (uploads, formulas, auth, API flows)
6. Fix batch 1: B01–B25, B30, B32–B38 confirmed bugs (each verified at runtime)
7. Wave 2b: github-projects, paper-finder, pdf tools, audio-studio, youtube, llm pages, notes, thermo
8. LOOP-BACK wave 3: full recrawl + regression of ALL pages → found+fixed music.html edit regression
9. Production verification: `npm run build` (3× green) + deploy.yml simulation (`cp -r assets/books/assets_for_my_website`) + preview crawl 29/29 + live-prod spot checks
10. Final verification wave: npm test, blog tests, build, node --check all edited JS

## Rev 2 — Terminal alignment task (2026-08-18)

1. Reproduce+measure alignment defect (geometry audit; could not view screenshot — model lacks image input)
2. Root-cause: read ALL terminal CSS (5 breakpoints + theme overrides) + JS COMMANDS/typewriter/echo
3. Fix: pre-wrap+word-break, explicit text-align:left, normalize 4 COMMANDS tables to fixed-width columns
4. Verify: per-column x-audit desktop/tablet/mobile × themes, wrap/overflow check, build, interactive regression
